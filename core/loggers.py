import logger

log_tasks = logger.logging.getLogger("Tasks")
log_commands = logger.logging.getLogger("Commands")
log_music = logger.logging.getLogger("Music")
log_http = logger.logging.getLogger("music_http")
log_ui = logger.logging.getLogger("UI")
log_events = logger.logging.getLogger("Events")
log_database = logger.logging.getLogger("Database")
